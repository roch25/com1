name: Dummy Multi-Job Workflow

on:
  push:
    branches:
      - main
  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Build project
        run: echo "Pretend we are building the project here..."

  test:
    runs-on: ubuntu-latest
    needs: build   # 👈 test job waits for build job to finish
    steps:
      - name: Run unit tests
        run: echo "Pretend we are running tests here..."

  deploy:
    runs-on: ubuntu-latest
    needs: test    # 👈 deploy runs only if test succeeds
    steps:
      - name: Deploy project
        run: echo "Pretend we are deploying the project here..."
